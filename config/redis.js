module.exports = {
    "redis-host": {
        help: "Redis host",
        metavar: "HOST",
        default: "127.0.0.1"
    },

    "redis-port": {
        help: "Redis port",
        metavar: "PORT",
        default: 6379
    }
}
