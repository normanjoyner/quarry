module.exports = {
    "mongo-host": {
        help: "MongoDB host",
        metavar: "HOST",
        default: "127.0.0.1"
    },

    "mongo-port": {
        help: "MongoDB port",
        metavar: "PORT",
        default: 27017
    },

    "mongo-database": {
        help: "MongoDB database",
        metavar: "DATABASE",
        default: "quarry"
    }
}
